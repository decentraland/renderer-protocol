syntax = "proto3";

message HUD {
  message FromKernel {
    oneof message {
      ConfigureHUDElement configure_hud_element = 1;
    }
    
    message ConfigureHUDElement {}
    /*
      (
      hudElementId: HUDController.HUDElementID,
      configuration: HUDConfiguration,
      extraPayload: any | null = null
    );*/
  }

  message FromRenderer {}
}
