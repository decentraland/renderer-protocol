syntax = "proto3";

message Emotes {
  message FromKernel
  {
    message TriggerSelfUserExpression {} //(string expressionId);
  }

  message FromRenderer
  {
    message TriggerExpression {} //(data: { id: string; timestamp: number });
  }
}
