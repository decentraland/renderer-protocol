syntax = "proto3";

message SDKComponents {
  message FromKernel {}

  message FromRenderer {
    message SetAudioStream {
      string url = 1;
      bool play = 2;
      float volume = 3;
    }
  }
}
