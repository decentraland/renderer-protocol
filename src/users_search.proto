syntax = "proto3";

message UsersSearch {
  message FromKernel {
    oneof message {
      SetENSOwnerQueryResult set_ens_owner_query_result = 1;
    }
    // ???
    message SetENSOwnerQueryResult {} //(searchInput: string, profiles: Profile[] | undefined);
  }

  message FromRenderer {
    oneof message {
      SearchENSOwner search_ens_owner = 1;
    }
    // Profile??
    message SearchENSOwner {} //(data: { name: string; maxResults?: number });
  }
}
